'use strict';

const number1 = +prompt('enter first number');
const number2 = +prompt('enter second number');
const number3 = +prompt('enter third number');

const averageValue = (number1 + number2 + number3) / 3;
  alert(averageValue);